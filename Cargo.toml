[package]
name = "kartei"
version = "0.0.0"
edition = "2024"
license = "AGPL-3.0-or-later"
repository = "https://github.com/espadat-studio/kartei"
description = "Keyboard-driven editor for contacts stored as vCard files in a local vdir."

[dev-dependencies]
proptest = "1.11.0"

[dependencies]
ratatui = "0.30.2"
